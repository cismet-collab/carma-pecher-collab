import { getLibVersion } from "./version";
import type { GetLibVersion } from "./types/version";
export const pecherCollabProjectId = "pecherCollabProject";
export const pecherCollabProjectVersion: GetLibVersion = getLibVersion();
